name: PingPlugin
version: 1.0
main: com.ping.PingPlugin
api-version: 1.20
softdepend: [PlaceholderAPI]

commands:
  ping:
    description: Show your ping
  setping:
    description: Set random ping
    permission: ping.admin

permissions:
  ping.admin:
    description: Admin Permissions
    default: op
